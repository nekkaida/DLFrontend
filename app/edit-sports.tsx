/**
 * Edit Sports Route — standalone sport selection for profile editing.
 * Does NOT use OnboardingContext and does NOT call updateOnboardingStep.
 */
export { EditSportsScreen as default } from '@features/profile/screens/EditSportsScreen';
